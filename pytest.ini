[pytest]
# pytest.ini — project-level Pytest configuration

# Default test discovery path
testpaths = tests

# Verbose output and short traceback for readability
addopts =
    -v
    --tb=short
    --html=reports/report.html
    --self-contained-html

# Log level captured during tests
log_cli = true
log_cli_level = INFO
